함수
1. 함수 정의하기
값
1) function 함수명(){

}

2) const 함수명 = function(){};

3) Function 

4) 화살표 함수
	- const 함수명 = (x,y) => x + y;
		- 생성자로써 사용 불가
		- this 범위가 다르고, 변경 불가

2. 중첩 함수
	

3. 프로그램 평가와 실행 과정
	- 실행 문맥(Execution Context) -> 스택 -> 함수 -> 실행 -> 실행문맥

	1. 환경 레코드
		- 원시타입 변수(숫자, 문자 ..., 함수)
		- 객체 변수
		- 외부 환경 레코드 참조

	2. this 범위

4. 자바스크립트는 싱글 스레드
	- 비동기 프로그래밍 방식

클로저

이름 공간

1. 객체를 이름 공간으로 활용하기
	- kakao
	- map

2. 모듈 패턴
	- 즉시 실행 함수

이터레이터 
	- prototype
		- .Symbol.iterator

	- for ... of 이터레이트
제너레이터
	- function * 함수명(){
		yield 반환값;
		yield 반환값;
	}







